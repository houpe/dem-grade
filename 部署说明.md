# 部署说明

整个项目文件夹可直接复制到其他电脑(含 Windows),双击启动脚本即可使用,无需手动配置环境。

## 一、环境要求

| 项 | 要求 |
|---|---|
| 操作系统 | Windows 10/11(64 位)或 macOS |
| Python | 3.9 ~ 3.12(64 位)。**项目已附带官方安装包 `安装包/python-3.12.10-amd64.exe`**(MD5 已与 python.org 核对):目标电脑没装 Python 时,双击 `启动.bat` 会自动打开它,勾选 "Add Python to PATH" 安装完成后脚本自动继续 |
| 网络 | 首次启动需联网(安装依赖约 1~3 分钟,已配置国内镜像);DEM 瓦片缺失时分析阶段也会自动联网补齐 |
| 磁盘 | 依赖约 200 MB;`data/dem` 会随使用增长(全国全量约 15~25 GB) |

## 二、快速开始(Windows)

1. 把整个项目文件夹复制到目标电脑(建议放到如 `D:\高程计算`)
2. 双击 **`启动.bat`**
3. 首次运行会自动:检测 Python(没有则打开附带安装包)→ 创建虚拟环境 → 安装依赖 → 启动服务 → 3 秒后自动打开浏览器
4. 之后的每次启动直接双击 `启动.bat`,约 2 秒起服务

关闭命令行窗口(或窗口内按 Ctrl+C)即停止服务。

## 三、从当前电脑打包迁移

在 macOS 上打包(排除虚拟环境等不可跨平台内容):

```bash
cd /Users/houpe/开发项目新
zip -r 高程计算.zip 高程计算 \
  -x "高程计算/.venv/*" "高程计算/__pycache__/*" "*.pyc" "高程计算/data/dem/*.part*"
```

说明:

- **`data/dem`(当前约 1.2 GB)建议一起打包**:目标电脑离线也能直接分析已覆盖区域;若想减小体积,可在上面的命令再加 `-x "高程计算/data/dem/*"`,目标电脑首次分析时会自动下载所需瓦片
- `.venv` 是本机编译的虚拟环境,**不可跨平台**,必须排除(Windows 脚本会自动重建)
- `data/boundaries`(行政边界缓存,几百 KB)建议保留

## 四、日常使用

- 服务地址:`http://127.0.0.1:8107`(仅本机访问,不触发 Windows 防火墙弹窗)
- 修改端口:编辑 `启动.bat` 里的 `set PORT=8107`
- 局域网访问(他人电脑也打开):把启动命令中 `--host 127.0.0.1` 改为 `--host 0.0.0.0`,并在 Windows 防火墙放行该端口;访问地址换成 `http://本机IP:8107`
- DEM 自动下载默认开启(`AUTO_DOWNLOAD=0` 可关闭);断网时只可分析本地已有瓦片区域

## 五、目录结构(哪些需要备份/迁移)

```
高程计算/
├── 启动.bat / 启动.command   # 启动脚本(Windows / macOS)
├── 安装包/                   # Python 官方安装器(Windows 用,26 MB)
├── server.py                 # 服务端(分析逻辑)
├── download_dem.py           # DEM 下载器(可选,全量下载时用)
├── static/                   # 网页界面(含本地化 Leaflet,离线可用)
├── requirements.txt          # Python 依赖清单
├── data/
│   ├── dem/                  # DEM 瓦片(核心数据,建议一起迁移)
│   └── boundaries/           # 行政边界缓存
├── .venv/                    # 虚拟环境(不迁移,自动重建)
└── 部署说明.md                # 本文件
```

## 六、常见问题

| 现象 | 处理 |
|---|---|
| 双击后提示"未找到 Python" | 一般不会出现(脚本会自动打开 `安装包/` 里的安装器);若手动安装,装完务必**重开命令行窗口**再试,并确认勾选过 Add to PATH |
| 依赖安装很慢/失败 | 脚本已用清华镜像;仍失败可检查代理,或手动执行 `.venv\Scripts\python -m pip install -r requirements.txt` |
| 端口被占用 | 改 `启动.bat` 里的 `set PORT=` 为其他端口(如 8200) |
| 首次分析某个新区域很慢 | 正在自动下载该区域 DEM 瓦片(每片 20~50 MB),下完后同区域秒级 |
| 分析报"轨迹覆盖 N 个缺失瓦片" | 自动下载被关闭或断网;联网重试,或运行 `python download_dem.py` 预先下载 |
| Windows SmartScreen 拦截 .bat | 点"更多信息"→"仍要运行"(脚本内容可先用记事本检查) |
| 上传后高程全为空 | 轨迹超出数据覆盖范围(仅支持经度 73~136°、纬度 17~54°) |

## 七、升级

只需把新版 `server.py`、`static/`、`requirements.txt` 覆盖到旧目录即可,`data/` 数据不受影响;若 `requirements.txt` 有变化,删掉 `.venv` 文件夹后重新双击启动脚本自动重建。
