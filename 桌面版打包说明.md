# 桌面版打包说明(Mac .app / Windows .exe)

桌面版 = 现在的 FastAPI 后端 + pywebview 原生窗口(系统自带 WebView,无需浏览器)。
PyInstaller 把 Python 运行时和全部依赖打进应用,**用户电脑不需要安装 Python 或任何环境**,解压双击即用。

- 桌面入口:`desktop.py`(后台起 uvicorn,默认端口 8107,被占用自动顺延 8108/8109;可用环境变量 `SLOPE_PORT` 指定)
- DEM 数据不打包,首次分析某区域时自动联网下载(Copernicus 30m,AWS 公开数据,无需账号)
  - macOS:`~/Library/Application Support/高程计算/dem`
  - Windows:`%LOCALAPPDATA%\高程计算\dem`

## Mac 版(在本机打包,已验证)

```bash
.venv/bin/pyinstaller desktop.py \
  --name SlopeAnalysis \
  --windowed --onedir \
  --add-data "static:static" \
  --collect-submodules rasterio \
  --collect-all certifi \
  --exclude-module tkinter \
  --icon icon.icns \
mv dist/SlopeAnalysis.app dist/高程计算.app   # 外层改中文名,内部可执行文件保持 ASCII
```

产物:`dist/高程计算.app`(约 95MB)。已完整验证:空数据目录 → 首次分析自动下载瓦片(弹窗进度条)→ 出结果;二次分析同区域秒回。

## Windows 版(必须在 Windows 电脑上打包)

PyInstaller 不支持交叉编译,Mac 上打不出 Windows exe。在任意一台 Windows 10/11(64 位)上:

1. 把整个项目文件夹拷过去(或 git 拉取;`dist/`、`.venv/`、`data/` 不用拷)
2. 安装 64 位 Python 3.10~3.12(勾选 "Add Python to PATH")——**这只是构建机的要求,最终用户不需要**
3. 双击运行 `build-windows.bat`,等它跑完

产物:`dist\SlopeAnalysis\` 文件夹(入口 `SlopeAnalysis.exe`)。**把整个文件夹压缩成 zip 发给用户**,用户解压后双击 exe 即可。

### 用户侧首次运行须知

- **SmartScreen 提示**:未签名 exe 首次运行会弹「已保护你的电脑」,点「更多信息」→「仍要运行」即可(想彻底消除需购买代码签名证书)
- **WebView2**:Win11 / 近年更新的 Win10 自带;若窗口空白,装一次[微软 WebView2 Runtime](https://developer.microsoft.com/microsoft-edge/webview2/) 即可
- **首次分析新区域**会弹窗显示下载进度(每片约 20~50 MB),下载一次保存在本机,之后同区域数秒出结果

## 已踩过的坑(改打包参数前必读)

| 坑 | 现象 | 对策(已体现在构建命令里) |
|---|---|---|
| `rasterio.serde` 未进包 | 启动即崩 `ModuleNotFoundError: rasterio.serde`(Cython 运行时动态导入,静态分析看不见) | `--collect-submodules rasterio` |
| CA 证书未进包 | 自动下载全部失败(502 网络问题),但开发环境正常 | `--collect-all certifi` + `desktop.py` 里设置 `SSL_CERT_FILE=certifi.where()` |
| 中文可执行文件名 | PyInstaller/签名链路对非 ASCII 名不稳 | 内部名保持 `SlopeAnalysis`,只改外层 `.app` 文件夹名 |
| `--add-data` 分隔符 | Mac 用 `:`,Windows 用 `;` | 两份命令已各自写对 |

## 与服务端模式的关系

`启动.command` / `启动.bat`(浏览器访问 8107)依然可用,两者共用同一份 `server.py` 和 `static/`。
桌面版只是多了一个 `desktop.py` 入口,业务逻辑零改动;改了 `server.py` / 前端后,重新打包一次桌面版才会带上新改动。
